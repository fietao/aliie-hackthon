version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=False
      - PORT=5000
      - HOST=0.0.0.0
    volumes:
      - .:/app
    restart: unless-stopped
